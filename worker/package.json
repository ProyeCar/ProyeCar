{
  "name": "proyecar-pdfs-worker",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy --env production"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
